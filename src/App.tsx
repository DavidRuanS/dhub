import { FormEvent, useEffect, useMemo, useState } from 'react'
import { currentEmail, signOut, updatePassword } from './lib/supabase'
import { emptyState, loadState, makeEvent, makeMaintenance, makeShopping, makeTask, makeTransaction, makeVehicle, persistState, today } from './data'
import type { AppState, EventItem, ShoppingItem, Tab, Vehicle } from './types'
import './extra.css'

type Modal = 'folder'|'shopping'|'vehicle'|'document'|'salary'|'transaction'|'task'|'event'|null
type Draft = Record<string,string>
type AppTab = Tab|'settings'

const brl=(v:number)=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})
const num=(v?:string)=>Number(String(v||0).replace(',','.'))||0
const fmt=(v:string|null)=>v?new Date(v+'T12:00:00').toLocaleDateString('pt-BR'):'Sem data'
const qty=(v:string)=>Number((v.match(/[\d.,]+/)?.[0]||'1').replace(',','.'))||1
const marker='__DHubFolder__'
const enhancements=`
.pageIntro{margin:-8px 0 16px;color:var(--m);font-size:12px}.folderGrid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.folderCard{position:relative;display:flex;gap:12px;align-items:center;background:#fff;border:1px solid var(--l);border-radius:18px;padding:15px;min-height:105px}.folderCard>div:nth-child(2){display:flex;flex-direction:column;gap:4px;min-width:0}.folderCard b{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.folderCard span{font-size:10px;color:var(--m)}.folderCard strong{color:var(--p)}.folderCard aside{position:absolute;top:7px;right:7px;display:flex}.folderCard aside button,.vehicleTop aside button{border:0;background:transparent;font-size:19px;color:#7b8c89}.folderCard aside button:last-child,.vehicleTop aside button:last-child{color:#b94f5b}.folderIcon{font-size:31px}.folderHeader{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:10px;margin-bottom:14px}.folderHeader h1,.folderHeader p{margin:0}.folderHeader p{font-size:10px;color:var(--m)}.folderHeader>button{border:0;background:var(--soft);color:var(--p);padding:10px;border-radius:11px}.liveTotal{display:flex;justify-content:space-between;align-items:center;background:var(--soft);padding:12px;border-radius:13px;margin-bottom:10px}.hint{font-size:11px;color:var(--m)}.calendar{background:#fff;border:1px solid var(--l);border-radius:20px;padding:15px}.calendar header{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px}.calendar header div{display:flex;flex-direction:column;gap:3px}.calendar header span{font-size:9px;color:var(--m)}.calendar header button{border:0;background:transparent;color:var(--p);font-size:10px}.week,.days{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;text-align:center}.week span{font-size:9px;color:var(--m);padding:5px}.days button,.days i{aspect-ratio:1;border:0;background:transparent;border-radius:11px;display:grid;place-items:center;font-size:11px}.days button.today{background:var(--p);color:#fff;font-weight:800}.days button.marked:not(.today){background:var(--soft);color:var(--p);font-weight:800;position:relative}.days button.marked:not(.today):after{content:'';width:4px;height:4px;border-radius:50%;background:var(--p);position:absolute;bottom:3px}.compact{max-height:220px}.menuProfile{display:flex;align-items:center;gap:10px;padding:8px 8px 18px}.menuProfile>span,.profileCard>span{width:48px;height:48px;border-radius:16px;background:var(--p);color:#fff;display:grid;place-items:center;font-weight:800;font-size:20px}.menuProfile div,.profileCard div{display:flex;flex-direction:column;gap:2px;min-width:0}.menuProfile small{font-size:9px;color:var(--m);overflow:hidden;text-overflow:ellipsis}.profileCard{display:flex;align-items:center;gap:13px;background:linear-gradient(145deg,var(--p),var(--pd));color:#fff;border-radius:22px;padding:18px}.profileCard>span{background:#ffffff25}.profileCard small{opacity:.8}.profileCard em{font-style:normal;font-size:9px;background:#ffffff20;padding:4px 7px;border-radius:8px;width:max-content}.settingRow{width:100%;display:flex;align-items:center;gap:12px;border:1px solid var(--l);background:#fff;border-radius:16px;padding:13px;margin-bottom:8px;text-align:left}.settingRow>i{width:39px;height:39px;border-radius:12px;background:var(--soft);color:var(--p);display:grid;place-items:center;font-style:normal;font-weight:800}.settingRow div{display:flex;flex:1;flex-direction:column;gap:3px}.settingRow span{font-size:9px;color:var(--m)}.settingRow>strong{font-size:22px;color:#9aa8a6}.dangerRow{color:#a83c48}.vehicleTop{display:flex;justify-content:space-between;gap:10px}.vehicleTop aside{display:flex}.sectionTitle{margin-top:22px}.emptyState{grid-column:1/-1}.field textarea{width:100%;min-height:75px;border:1px solid var(--l);border-radius:12px;padding:10px;resize:vertical}@media(max-width:480px){.folderGrid{grid-template-columns:1fr}.calendar{padding:12px}}
`
const isMarker=(x:ShoppingItem)=>x.name===marker
const nav:[AppTab,string,string][]=[['home','Início','⌂'],['tasks','Tarefas','✓'],['finance','Finanças','R$'],['car','Veículos','🚗'],['market','Mercado','🛒']]

export default function App(){
 const[s,setS]=useState<AppState>(emptyState),[ready,setReady]=useState(false),[tab,setTab]=useState<AppTab>('home'),[menu,setMenu]=useState(false)
 const[modal,setModal]=useState<Modal>(null),[d,setD]=useState<Draft>({}),[error,setError]=useState(''),[email,setEmail]=useState(''),[folder,setFolder]=useState<string|null>(null)
 useEffect(()=>{loadState().then(x=>{setS(x);setReady(true)});currentEmail().then(setEmail)},[])
 useEffect(()=>{if(!ready)return;const t=setTimeout(()=>persistState(s),450);return()=>clearTimeout(t)},[s,ready])
 const up=(fn:(x:AppState)=>AppState)=>setS(x=>fn(x)),month=today().slice(0,7)
 const monthTx=s.transactions.filter(x=>x.transaction_date.startsWith(month))
 const income=monthTx.filter(x=>x.type==='income'&&x.status==='paid').reduce((a,x)=>a+Number(x.amount),0)+Number(s.settings.monthly_salary||0)
 const spent=monthTx.filter(x=>x.type==='expense'&&x.status==='paid').reduce((a,x)=>a+Number(x.amount),0)
 const pending=monthTx.filter(x=>x.type==='expense'&&x.status==='pending').reduce((a,x)=>a+Number(x.amount),0),balance=income-spent-pending
 const folders=useMemo(()=>Array.from(new Set(s.shopping.map(x=>x.category||'Lista principal'))).sort(),[s.shopping])
 const items=s.shopping.filter(x=>!isMarker(x)&&(x.category||'Lista principal')===folder)
 const listTotal=items.reduce((a,x)=>a+qty(x.quantity)*Number(x.estimated_price||0),0)
 const docs=s.maintenance.filter(x=>x.title.startsWith('Documento:'))
 const alerts=docs.filter(x=>x.next_date&&!x.notes.includes('[PAGO]')).map(x=>({...x,days:Math.ceil((new Date(x.next_date!+'T12:00:00').getTime()-Date.now())/86400000)})).filter(x=>x.days<=120).sort((a,b)=>a.days-b.days)
 const upcoming=s.events.filter(x=>x.event_date>=today()).sort((a,b)=>a.event_date.localeCompare(b.event_date)).slice(0,4)
 if(!ready)return <div className="load"><i>D</i><b>Carregando DHub...</b></div>
 const open=(m:Modal,seed:Draft={})=>{setModal(m);setD(seed);setError('')},close=()=>{setModal(null);setD({});setError('')},field=(k:string,v:string)=>setD(x=>({...x,[k]:v}))
 const remove=(key:'tasks'|'events'|'transactions'|'maintenance'|'shopping'|'vehicles',id:string)=>{if(confirm('Excluir este registro?'))up(x=>({...x,[key]:x[key].filter(i=>i.id!==id)} as AppState))}
 const deleteFolder=(name:string)=>{if(confirm(`Excluir a pasta “${name}” e todos os produtos dela?`)){up(x=>({...x,shopping:x.shopping.filter(i=>(i.category||'Lista principal')!==name)}));setFolder(null)}}
 function save(e:FormEvent){e.preventDefault();const id=d.id
  if(modal==='folder'){if(!d.name?.trim())return setError('Informe o nome da pasta.');const old=d.oldName,newName=d.name.trim();if(folders.includes(newName)&&newName!==old)return setError('Já existe uma pasta com esse nome.');if(old){up(x=>({...x,shopping:x.shopping.map(i=>(i.category||'Lista principal')===old?{...i,category:newName}:i)}))}else{up(x=>({...x,shopping:[...x.shopping,makeShopping({name:marker,quantity:'0 un.',estimated_price:0,category:newName})]}))}setFolder(newName)}
  if(modal==='shopping'){if(!d.name?.trim()||num(d.quantity)<=0)return setError('Preencha produto e quantidade.');const q=num(d.quantity),p=num(d.price),category=d.folder||folder||'Lista principal',quantityText=`${q} ${d.unit||'un.'}`;up(x=>({...x,shopping:id?x.shopping.map(i=>i.id===id?{...i,name:d.name.trim(),quantity:quantityText,estimated_price:p,category}:i):[...x.shopping,makeShopping({name:d.name.trim(),quantity:quantityText,estimated_price:p,category})]}));setFolder(category)}
  if(modal==='vehicle'){if(!d.name||!d.model||!d.year)return setError('Preencha apelido, modelo e ano.');up(x=>({...x,vehicles:id?x.vehicles.map(i=>i.id===id?{...i,nickname:d.name,model:d.model,year:d.year,mileage:num(d.km)}:i):[...x.vehicles,makeVehicle({nickname:d.name,model:d.model,year:d.year,mileage:num(d.km)})]}))}
  if(modal==='document'){if(!d.vehicle||!d.type||!d.date)return setError('Escolha veículo, documento e vencimento.');const title=`Documento: ${d.type}`,notes=`${d.paid==='yes'?'[PAGO]':'[PENDENTE]'} ${d.notes||''}`.trim();up(x=>({...x,maintenance:id?x.maintenance.map(i=>i.id===id?{...i,vehicle_id:d.vehicle,title,cost:num(d.amount),next_date:d.date,notes}:i):[...x.maintenance,makeMaintenance({vehicle_id:d.vehicle,title,cost:num(d.amount),performed_date:today(),performed_mileage:null,next_date:d.date,next_mileage:null,notes})]}))}
  if(modal==='salary'){if(num(d.salary)<=0)return setError('Informe o salário.');up(x=>({...x,settings:{...x.settings,monthly_salary:num(d.salary),payday:Math.min(31,Math.max(1,num(d.day)))}}))}
  if(modal==='transaction'){if(!d.description||num(d.amount)<=0)return setError('Informe descrição e valor.');up(x=>({...x,transactions:[...x.transactions,makeTransaction({type:d.type==='income'?'income':'expense',description:d.description,amount:num(d.amount),category:d.category||'Outros',transaction_date:d.date||today(),status:d.status==='pending'?'pending':'paid'})]}))}
  if(modal==='task'){if(!d.title)return setError('Informe a tarefa.');up(x=>({...x,tasks:[...x.tasks,makeTask({title:d.title,due_date:d.date||today(),due_time:d.allDay==='yes'?null:d.time||null,category:d.category||'Pessoal'})]}))}
  if(modal==='event'){if(!d.title||!d.date)return setError('Informe compromisso e data.');const notes=[d.location?`Local: ${d.location}`:'',d.notes||''].filter(Boolean).join(' | ');up(x=>({...x,events:[...x.events,makeEvent({title:d.title,event_date:d.date,event_time:d.allDay==='yes'?null:d.time||null,category:d.category||'Pessoal',notes})]}))}
  close()
 }
 return <div className="app"><style>{enhancements}</style><header><button className="iconButton" onClick={()=>setMenu(true)}>☰</button><div className="brand"><b>DHub</b><small>Sincronizado</small></div><button className="avatar" onClick={()=>setTab('settings')}>{s.settings.display_name[0]||'D'}</button></header>
 {menu&&<div className="shade" onClick={()=>setMenu(false)}><aside onClick={e=>e.stopPropagation()}><div className="menuProfile"><span>{s.settings.display_name[0]||'D'}</span><div><b>{s.settings.display_name}</b><small>{email}</small></div></div>{[...nav,['agenda','Agenda','▦'] as [AppTab,string,string],['settings','Configurações','⚙'] as [AppTab,string,string]].map(([id,l,i])=><button key={id} className={tab===id?'on':''} onClick={()=>{setTab(id);setMenu(false)}}><i>{i}</i>{l}<span>›</span></button>)}</aside></div>}
 <main>
 {tab==='home'&&<section><h1>Olá, {s.settings.display_name} 👋</h1><article className="balance"><span>Saldo previsto do mês</span><b className={balance<0?'red':''}>{brl(balance)}</b><small>{brl(income)} entradas · {brl(spent+pending)} compromissos</small></article>{alerts.length>0&&<><SectionTitle title="Alertas dos veículos" sub="Vencimentos que precisam de atenção"/>{alerts.slice(0,3).map(x=><Alert key={x.id} title={x.title.replace('Documento: ','')} text={`${x.days<0?'Vencido':`Vence em ${x.days} dias`} · ${fmt(x.next_date)}`}/>)}</>}<SectionTitle title="Calendário" sub="Compromissos deste mês"/><Calendar events={s.events} onAgenda={()=>setTab('agenda')}/><SectionTitle title="Próximos compromissos" sub="Sua agenda mais próxima"/><div className="list compact">{upcoming.map(x=><Row key={x.id} title={x.title} sub={`${fmt(x.event_date)} · ${x.event_time||'Dia todo'} · ${x.category}`}/>) }{!upcoming.length&&<Empty text="Nenhum compromisso próximo."/>}</div><SectionTitle title="Atalhos" sub="Acesso rápido"/><div className="grid"><button onClick={()=>open('transaction',{type:'expense',date:today()})}>R$<span>Movimentação</span></button><button onClick={()=>open('task',{date:today(),allDay:'yes'})}>✓<span>Tarefa</span></button><button onClick={()=>open('event',{date:today(),allDay:'yes'})}>▦<span>Agenda</span></button><button onClick={()=>setTab('market')}>🛒<span>Mercado</span></button></div></section>}
 {tab==='finance'&&<section><Page title="Finanças" add={()=>open('transaction',{type:'expense',date:today()})}/><button className="salary" onClick={()=>open('salary',{salary:String(s.settings.monthly_salary||''),day:String(s.settings.payday)})}><span>Salário fixo mensal</span><b>{s.settings.monthly_salary?brl(s.settings.monthly_salary):'Definir salário'}</b><small>Entrada prevista todo dia {s.settings.payday}</small></button><div className="summary"><div><span>Entradas</span><b>{brl(income)}</b></div><div><span>Saídas</span><b>{brl(spent)}</b></div><div><span>Pendentes</span><b>{brl(pending)}</b></div></div><List>{monthTx.slice().reverse().map(x=><Row key={x.id} title={x.description} sub={`${x.category} · ${fmt(x.transaction_date)}`} value={`${x.type==='expense'?'-':'+'}${brl(x.amount)}`} del={()=>remove('transactions',x.id)}/>)}</List></section>}
 {tab==='market'&&<section>{!folder?<><Page title="Pastas de compras" add={()=>open('folder')}/><p className="pageIntro">Crie uma pasta para cada pessoa, mês ou ocasião.</p><div className="folderGrid">{folders.map(name=>{const xs=s.shopping.filter(i=>!isMarker(i)&&(i.category||'Lista principal')===name),total=xs.reduce((a,x)=>a+qty(x.quantity)*Number(x.estimated_price||0),0);return <article className="folderCard" key={name} onClick={()=>setFolder(name)}><div className="folderIcon">📁</div><div><b>{name}</b><span>{xs.length} itens</span><strong>{brl(total)}</strong></div><aside><button onClick={e=>{e.stopPropagation();open('folder',{oldName:name,name})}}>✎</button><button onClick={e=>{e.stopPropagation();deleteFolder(name)}}>×</button></aside></article>})}{!folders.length&&<Empty text="Nenhuma pasta criada. Toque no + para começar."/>}</div></>:<><div className="folderHeader"><button onClick={()=>setFolder(null)}>‹ Pastas</button><div><h1>{folder}</h1><p>{items.length} itens na lista</p></div><button onClick={()=>open('folder',{oldName:folder,name:folder})}>✎</button></div><article className="marketTotal"><span>Total estimado</span><b>{brl(listTotal)}</b><small>Atualizado automaticamente por quantidade × preço</small><button onClick={()=>open('shopping',{folder,quantity:'1',unit:'un.'})}>＋ Adicionar produto</button></article><List>{items.map(x=><Row key={x.id} title={x.name} sub={`${x.quantity} × ${brl(Number(x.estimated_price||0))}`} value={brl(qty(x.quantity)*Number(x.estimated_price||0))} checked={x.purchased} toggle={()=>up(a=>({...a,shopping:a.shopping.map(i=>i.id===x.id?{...i,purchased:!i.purchased}:i)}))} edit={()=>open('shopping',{id:x.id,name:x.name,quantity:String(qty(x.quantity)),unit:x.quantity.replace(/[\d.,\s]/g,'')||'un.',price:String(x.estimated_price||''),folder:x.category})} del={()=>remove('shopping',x.id)}/>) }{!items.length&&<Empty text="Esta pasta ainda não tem produtos."/>}</List></>}</section>}
 {tab==='car'&&<section><Page title="Veículos" add={()=>open('vehicle')}/>{alerts.map(x=><Alert key={x.id} title={x.title.replace('Documento: ','')} text={`${x.days<0?'Vencido':`Vence em ${x.days} dias`} · ${fmt(x.next_date)}`}/>)}{s.vehicles.map(v=><VehicleCard key={v.id} v={v} docs={docs} open={open} remove={remove}/>) }{!s.vehicles.length&&<Empty text="Cadastre seu carro ou sua moto."/>}</section>}
 {tab==='tasks'&&<section><Page title="Tarefas" add={()=>open('task',{date:today(),allDay:'yes'})}/><List>{s.tasks.map(x=><Row key={x.id} title={x.title} sub={`${fmt(x.due_date)} · ${x.due_time||'Dia todo'} · ${x.category}`} del={()=>remove('tasks',x.id)}/>)}</List></section>}
 {tab==='agenda'&&<section><Page title="Agenda" add={()=>open('event',{date:today(),allDay:'yes'})}/><Calendar events={s.events}/><SectionTitle title="Compromissos" sub="Eventos salvos"/><List>{s.events.slice().sort((a,b)=>a.event_date.localeCompare(b.event_date)).map(x=><Row key={x.id} title={x.title} sub={`${fmt(x.event_date)} · ${x.event_time||'Dia todo'} · ${x.category}${x.notes?` · ${x.notes}`:''}`} del={()=>remove('events',x.id)}/>)}</List></section>}
 {tab==='settings'&&<section><h1>Configurações</h1><div className="profileCard"><span>{s.settings.display_name[0]||'D'}</span><div><b>{s.settings.display_name}</b><small>{email}</small><em>Conta sincronizada</em></div></div><SectionTitle title="Perfil" sub="Informações da sua conta"/><article className="settings"><label>Nome de exibição<input value={s.settings.display_name} onChange={e=>up(x=>({...x,settings:{...x.settings,display_name:e.target.value}}))}/></label><label>E-mail da conta<input value={email} disabled/></label></article><SectionTitle title="Finanças" sub="Renda mensal automática"/><button className="settingRow" onClick={()=>open('salary',{salary:String(s.settings.monthly_salary||''),day:String(s.settings.payday)})}><i>R$</i><div><b>Salário fixo</b><span>{s.settings.monthly_salary?`${brl(s.settings.monthly_salary)} · dia ${s.settings.payday}`:'Não configurado'}</span></div><strong>›</strong></button><SectionTitle title="Segurança" sub="Acesso ao DHub"/><button className="settingRow" onClick={async()=>{const p=prompt('Nova senha (mínimo 6 caracteres):');if(p&&p.length>=6){await updatePassword(p);alert('Senha alterada.')}}}><i>🔑</i><div><b>Alterar senha</b><span>Defina uma nova senha para sua conta</span></div><strong>›</strong></button><button className="settingRow" onClick={async()=>{if(!('Notification'in window))return alert('Este navegador não oferece notificações.');const p=await Notification.requestPermission();alert(p==='granted'?'Notificações ativadas.':'Permissão não concedida.')}}><i>🔔</i><div><b>Notificações</b><span>Permitir lembretes no navegador</span></div><strong>›</strong></button><button className="settingRow dangerRow" onClick={()=>signOut()}><i>↪</i><div><b>Sair da conta</b><span>Encerrar a sessão neste aparelho</span></div><strong>›</strong></button></section>}
 </main><nav>{nav.map(([id,l,i])=><button className={tab===id?'on':''} onClick={()=>setTab(id)} key={id}><b>{i}</b><small>{l}</small></button>)}</nav>
 {modal&&<div className="modalShade" onClick={close}><form onSubmit={save} onClick={e=>e.stopPropagation()} className="modal"><div className="modalHead"><div><h2>{d.id?'Editar':'Novo'} registro</h2><p>Preencha os dados principais.</p></div><button type="button" onClick={close}>×</button></div><Fields modal={modal} d={d} set={field} state={s}/>{error&&<div className="formError">⚠ {error}</div>}<footer><button type="button" onClick={close}>Cancelar</button><button>Salvar</button></footer></form></div>}</div>
}
function VehicleCard({v,docs,open,remove}:{v:Vehicle,docs:AppState['maintenance'],open:(m:Modal,d?:Draft)=>void,remove:(k:'maintenance'|'vehicles',id:string)=>void}){return <article className="vehicle"><div className="vehicleTop"><div><h2>{v.nickname}</h2><span>{v.model} · {v.year}</span><small>{v.mileage.toLocaleString('pt-BR')} km</small></div><aside><button onClick={()=>open('vehicle',{id:v.id,name:v.nickname,model:v.model,year:v.year,km:String(v.mileage)})}>✎</button><button onClick={()=>remove('vehicles',v.id)}>×</button></aside></div><button onClick={()=>open('document',{vehicle:v.id,type:'IPVA',date:today(),paid:'no'})}>＋ IPVA, licenciamento ou seguro</button>{docs.filter(x=>x.vehicle_id===v.id).map(x=><Row key={x.id} title={x.title.replace('Documento: ','')} sub={`${fmt(x.next_date)} · ${x.notes.includes('[PAGO]')?'Pago':'Pendente'}`} value={x.cost?brl(x.cost):''} edit={()=>open('document',{id:x.id,vehicle:x.vehicle_id,type:x.title.replace('Documento: ',''),date:x.next_date||'',amount:String(x.cost||''),paid:x.notes.includes('[PAGO]')?'yes':'no',notes:x.notes.replace('[PAGO]','').replace('[PENDENTE]','').trim()})} del={()=>remove('maintenance',x.id)}/>)}</article>}
function Page({title,add}:{title:string,add:()=>void}){return <div className="page"><h1>{title}</h1><button onClick={add}>＋</button></div>}
function SectionTitle({title,sub}:{title:string,sub:string}){return <div className="sectionTitle"><div><h2>{title}</h2><p>{sub}</p></div></div>}
function List({children}:{children:React.ReactNode}){return <div className="list">{children}</div>}
function Empty({text}:{text:string}){return <div className="emptyState"><i>＋</i><b>{text}</b></div>}
function Alert({title,text}:{title:string,text:string}){return <div className="alert"><b>{title}</b><span>{text}</span></div>}
function Row({title,sub,value,edit,del,checked,toggle}:{title:string,sub:string,value?:string,edit?:()=>void,del?:()=>void,checked?:boolean,toggle?:()=>void}){return <article className={`row ${checked?'done':''}`}>{toggle&&<button className="check" onClick={toggle}>{checked?'✓':'○'}</button>}<div><b>{title}</b><small>{sub}</small></div>{value&&<strong>{value}</strong>}<aside>{edit&&<button onClick={edit}>✎</button>}{del&&<button onClick={del}>×</button>}</aside></article>}
function Calendar({events,onAgenda}:{events:EventItem[],onAgenda?:()=>void}){const now=new Date(),y=now.getFullYear(),m=now.getMonth(),first=new Date(y,m,1).getDay(),count=new Date(y,m+1,0).getDate(),prefix=`${y}-${String(m+1).padStart(2,'0')}`,marked=new Set(events.filter(e=>e.event_date.startsWith(prefix)).map(e=>Number(e.event_date.slice(8,10))));return <article className="calendar"><header><div><b>{now.toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</b><span>{marked.size} dias com eventos</span></div>{onAgenda&&<button onClick={onAgenda}>Ver agenda ›</button>}</header><div className="week">{'DSTQQSS'.split('').map((x,i)=><span key={i}>{x}</span>)}</div><div className="days">{Array.from({length:first}).map((_,i)=><i key={'e'+i}/>)}{Array.from({length:count},(_,i)=>i+1).map(day=><button key={day} className={`${day===now.getDate()?'today':''} ${marked.has(day)?'marked':''}`} onClick={onAgenda}>{day}</button>)}</div></article>}
function Fields({modal,d,set,state}:{modal:Exclude<Modal,null>,d:Draft,set:(k:string,v:string)=>void,state:AppState}){if(modal==='folder')return <><F l="Nome da pasta"><input value={d.name||''} placeholder="Ex.: Compras da Bruna - Julho" onChange={e=>set('name',e.target.value)}/></F><p className="hint">Use uma pasta para cada pessoa, mês ou ocasião.</p></>;if(modal==='shopping')return <><F l="Produto"><input value={d.name||''} onChange={e=>set('name',e.target.value)}/></F><div className="two"><F l="Quantidade"><input inputMode="decimal" value={d.quantity||''} onChange={e=>set('quantity',e.target.value)}/></F><F l="Unidade"><select value={d.unit||'un.'} onChange={e=>set('unit',e.target.value)}><option>un.</option><option>kg</option><option>g</option><option>L</option><option>pct.</option><option>cx.</option></select></F></div><F l="Preço unitário"><input inputMode="decimal" value={d.price||''} onChange={e=>set('price',e.target.value)}/></F><F l="Pasta"><input value={d.folder||''} onChange={e=>set('folder',e.target.value)}/></F><div className="liveTotal"><span>Total do item</span><b>{brl(num(d.quantity)*num(d.price))}</b></div></>;if(modal==='vehicle')return <><F l="Apelido"><input value={d.name||''} placeholder="Ex.: Minha Spin" onChange={e=>set('name',e.target.value)}/></F><F l="Modelo"><input value={d.model||''} onChange={e=>set('model',e.target.value)}/></F><div className="two"><F l="Ano"><input value={d.year||''} onChange={e=>set('year',e.target.value)}/></F><F l="Quilometragem"><input inputMode="numeric" value={d.km||''} onChange={e=>set('km',e.target.value)}/></F></div></>;if(modal==='document')return <><F l="Veículo"><select value={d.vehicle||''} onChange={e=>set('vehicle',e.target.value)}><option value="">Selecione</option>{state.vehicles.map(v=><option key={v.id} value={v.id}>{v.nickname}</option>)}</select></F><F l="Tipo"><select value={d.type||'IPVA'} onChange={e=>set('type',e.target.value)}><option>IPVA</option><option>Licenciamento</option><option>Seguro</option><option>Revisão</option><option>Outro</option></select></F><div className="two"><F l="Vencimento"><input type="date" value={d.date||''} onChange={e=>set('date',e.target.value)}/></F><F l="Valor (opcional)"><input inputMode="decimal" value={d.amount||''} onChange={e=>set('amount',e.target.value)}/></F></div><F l="Situação"><select value={d.paid||'no'} onChange={e=>set('paid',e.target.value)}><option value="no">Pendente</option><option value="yes">Pago</option></select></F><F l="Observações"><textarea value={d.notes||''} onChange={e=>set('notes',e.target.value)}/></F></>;if(modal==='salary')return <><F l="Salário mensal"><input inputMode="decimal" value={d.salary||''} onChange={e=>set('salary',e.target.value)}/></F><F l="Dia do pagamento"><input inputMode="numeric" value={d.day||''} onChange={e=>set('day',e.target.value)}/></F></>;if(modal==='transaction')return <><F l="Tipo"><select value={d.type||'expense'} onChange={e=>set('type',e.target.value)}><option value="expense">Saída</option><option value="income">Entrada</option></select></F><F l="Descrição"><input value={d.description||''} onChange={e=>set('description',e.target.value)}/></F><div className="two"><F l="Valor"><input inputMode="decimal" value={d.amount||''} onChange={e=>set('amount',e.target.value)}/></F><F l="Data"><input type="date" value={d.date||''} onChange={e=>set('date',e.target.value)}/></F></div><F l="Categoria"><input value={d.category||''} onChange={e=>set('category',e.target.value)}/></F><F l="Situação"><select value={d.status||'paid'} onChange={e=>set('status',e.target.value)}><option value="paid">Pago/recebido</option><option value="pending">Pendente</option></select></F></>;return <><F l={modal==='task'?'Tarefa':'Compromisso'}><input value={d.title||''} onChange={e=>set('title',e.target.value)}/></F><F l="Data"><input type="date" value={d.date||''} onChange={e=>set('date',e.target.value)}/></F><label className="all"><input type="checkbox" checked={d.allDay==='yes'} onChange={e=>set('allDay',e.target.checked?'yes':'no')}/> Dia todo</label>{d.allDay!=='yes'&&<F l="Horário"><input type="time" value={d.time||''} onChange={e=>set('time',e.target.value)}/></F>}<F l="Categoria"><input value={d.category||''} onChange={e=>set('category',e.target.value)}/></F>{modal==='event'&&<><F l="Local"><input value={d.location||''} onChange={e=>set('location',e.target.value)}/></F><F l="Observações"><textarea value={d.notes||''} onChange={e=>set('notes',e.target.value)}/></F></>}</>}
function F({l,children}:{l:string,children:React.ReactNode}){return <label className="field"><span>{l}</span>{children}</label>}